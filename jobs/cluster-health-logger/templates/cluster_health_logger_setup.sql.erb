SET SESSION SQL_LOG_BIN = off;
CREATE USER IF NOT EXISTS 'cluster-health-logger'@'localhost' IDENTIFIED BY '<%= p('db_password') %>';
ALTER USER IF EXISTS 'cluster-health-logger'@'localhost' IDENTIFIED BY '<%= p('db_password') %>';
SET SESSION SQL_LOG_BIN = on;
