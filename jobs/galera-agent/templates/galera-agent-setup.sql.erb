SET SESSION SQL_LOG_BIN = off;
DROP USER IF EXISTS 'galera-agent'@'127.0.0.1';
GRANT USAGE ON mysql.* TO 'galera-agent'@'localhost' IDENTIFIED BY '<%= p('db_password') %>';
SET SESSION SQL_LOG_BIN = on;
