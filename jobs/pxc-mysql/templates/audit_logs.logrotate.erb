/var/vcap/store/mysql_audit_logs/mysql_server_audit.log {
  missingok
  rotate <%= p('engine_config.audit_logs.file_rotations') %>
  compress
  delaycompress
  copytruncate
  size=<%= p('engine_config.audit_logs.rotate_size_in_mb') %>M
}
