#!/bin/bash

set -euo pipefail

<% if spec.bootstrap %>
  echo "Started bootstrap errand ..."
  /var/vcap/jobs/bpm/bin/bpm run bootstrap
  echo "Bootstrap errand completed"
<% else %>
  echo "This errand only runs on the bootstrap node"
<% end %>
exit 0