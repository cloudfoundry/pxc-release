set -e

source /var/vcap/packages/golang-1-linux/bosh/compile.env

mkdir -p percona-toolkit

mkdir -p "${BOSH_INSTALL_TARGET}"/bin

tar -C percona-toolkit -xzf ./percona-toolkit-*.tar.gz

cd ./percona-toolkit/percona-toolkit-* \
    && cp bin/pt-mysql-summary ${BOSH_INSTALL_TARGET}/bin \
    && cp bin/pt-summary ${BOSH_INSTALL_TARGET}/bin \
    && cp bin/pt-stalk ${BOSH_INSTALL_TARGET}/bin

